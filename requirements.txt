text
# Core Framework
torch>=1.12.0,<2.3.0
torchvision>=0.13.0,<0.18.0
ultralytics>=8.0.0,<8.3.0

# Data Processing & Augmentation
opencv-python>=4.8.0,<4.10.0
pillow>=9.5.0
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
tqdm>=4.65.0

# Image & Multimodal Processing
albumentations>=1.3.0
scikit-image>=0.21.0

# Visualization & Logging
matplotlib>=3.7.0
seaborn>=0.12.0
tensorboard>=2.13.0

# Dataset & Utilities
pyyaml>=6.0
requests>=2.28.0
psutil>=5.9.0

# Optional: Edge Deployment
onnx>=1.14.0
onnxruntime-gpu>=1.16.0
tensorrt>=10.0.0  # NVIDIA TensorRT (requires CUDA)

# Optional: Advanced Features
thop>=0.1.1          # FLOPs computation
wandb>=0.15.0        # Weights & Biases logging
Installation Instructions
CPU-only (Basic)
bash
pip install -r requirements-cpu.txt
GPU (Recommended)
bash
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
pip install -r requirements.txt
Edge Deployment
bash
pip install onnx onnxruntime-gpu tensorrt
Development (Full)
bash
pip install -r requirements-dev.txt  # Includes testing, linting
requirements-cpu.txt (Lightweight)
text
torch>=1.12.0,<2.3.0
torchvision>=0.13.0,<0.18.0
ultralytics>=8.0.0,<8.3.0
opencv-python-headless>=4.8.0
pillow>=9.5.0
numpy>=1.24.0
pyyaml>=6.0
tqdm>=4.65.0
requirements-dev.txt (Complete)
text
-r requirements.txt
pytest>=7.4.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0
coverage>=7.0.0
CUDA Compatibility Matrix
PyTorch	CUDA	TensorRT
PyTorch	CUDA	TensorRT
2.2.x	11.8	10.0+
2.1.x	11.8	9.0+
2.0.x	11.8	8.6+
Recommended: PyTorch 2.1.0 + CUDA 11.8 for NVIDIA T4/A100

Troubleshooting
text
# CUDA out of memory
export PYTORCH_CUDA_ALLOC_CONF=max_split_size_mb:128

# Slow training
python train.py --workers 4 --batch 8 --amp=True

# Import errors
pip install --upgrade ultralytics
